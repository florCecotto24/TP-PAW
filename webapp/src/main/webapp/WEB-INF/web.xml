<?xml version="1.0" encoding="UTF-8"?>
<web-app
    version="2.4" id="PAW"
    xmlns="http://java.sun.com/xml/ns/j2ee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">
  <display-name>PAW webapp 2026a</display-name>

  <context-param>
    <param-name>contextClass</param-name>
    <param-value>
      org.springframework.web.context.support.AnnotationConfigWebApplicationContext
    </param-value>
  </context-param>

  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>
      ar.edu.itba.paw.webapp.config.WebConfig
    </param-value>
  </context-param>

  <listener>
    <listener-class>
      org.springframework.web.context.ContextLoaderListener
    </listener-class>
  </listener>

  <listener>
    <listener-class>ar.edu.itba.paw.webapp.listener.JdbcDriverDeregistrationListener</listener-class>
  </listener>

  <listener>
    <listener-class>ar.edu.itba.paw.webapp.listener.SessionTrackingDisablingListener</listener-class>
  </listener>

  <!-- 1. La API REST la atiende el filtro Jersey bajo /api/* (recursos JAX-RS en /users, /cars, …). -->

  <filter>
    <filter-name>spaFallbackFilter</filter-name>
    <filter-class>ar.edu.itba.paw.webapp.filter.SpaFallbackFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>spaFallbackFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>unconditionalCacheFilter</filter-name>
    <filter-class>ar.edu.itba.paw.webapp.filter.UnconditionalCacheFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>unconditionalCacheFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
    <dispatcher>FORWARD</dispatcher>
  </filter-mapping>

  <!-- Request UTF-8 only: do not force response charset (breaks image/* + nosniff in <img>). -->
  <filter>
    <filter-name>characterEncodingFilter</filter-name>
    <filter-class>ar.edu.itba.paw.webapp.filter.Utf8RequestEncodingFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>characterEncodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>rydenLocaleFilter</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>rydenLocaleFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>openEntityManagerInViewFilter</filter-name>
    <filter-class>org.springframework.orm.jpa.support.OpenEntityManagerInViewFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>openEntityManagerInViewFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>springSecurityFilterChain</filter-name>
    <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>springSecurityFilterChain</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>jersey</filter-name>
    <filter-class>org.glassfish.jersey.servlet.ServletContainer</filter-class>
    <init-param>
      <param-name>jersey.config.server.provider.packages</param-name>
      <param-value>ar.edu.itba.paw.webapp.controller,ar.edu.itba.paw.webapp.exception.mapper,ar.edu.itba.paw.webapp.config</param-value>
    </init-param>
    <init-param>
      <param-name>jersey.config.server.provider.classnames</param-name>
      <param-value>org.glassfish.jersey.media.multipart.MultiPartFeature,org.glassfish.jersey.server.validation.ValidationFeature,ar.edu.itba.paw.webapp.config.SpringValidatorBinder</param-value>
    </init-param>
    <init-param>
      <param-name>jersey.config.servlet.filter.forwardOn404</param-name>
      <param-value>false</param-value>
    </init-param>
    <init-param>
      <!-- Strip /api so @Path("/cars") matches /api/cars when the matched servlet is default "/". -->
      <param-name>jersey.config.servlet.filter.contextPath</param-name>
      <param-value>/api</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>jersey</filter-name>
    <url-pattern>/api/*</url-pattern>
  </filter-mapping>

  <!--
    Catch-all so Tomcat runs the filter chain on /api/* and SPA deep-links.
    Without this, unmatched paths 404 before SpaFallback/Jersey. Do not restore the old
    jsp-file /* mapping — it reintroduces JSESSIONID.
  -->
  <servlet-mapping>
    <servlet-name>default</servlet-name>
    <url-pattern>/</url-pattern>
  </servlet-mapping>

  <!-- 2. Archivos estáticos -> servlet por defecto -->
  <servlet-mapping>
    <servlet-name>default</servlet-name>
    <url-pattern>/assets/*</url-pattern>
  </servlet-mapping>
  <servlet-mapping>
    <servlet-name>default</servlet-name>
    <url-pattern>/static/*</url-pattern>
  </servlet-mapping>
  <servlet-mapping>
    <servlet-name>default</servlet-name>
    <url-pattern>/public/*</url-pattern>
  </servlet-mapping>
  <servlet-mapping>
    <servlet-name>default</servlet-name>
    <url-pattern>/index.html</url-pattern>
  </servlet-mapping>

  <!-- Deep-links SPA: SpaFallbackFilter forwards HTML navigations to /index.html (default servlet). -->

</web-app>
